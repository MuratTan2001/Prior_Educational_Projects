This project implements a CNN-based gender classification system using TensorFlow and OpenCV. 
Images are loaded from male/female folders, resized, normalized, and split into training and test sets. 
The model is trained, evaluated, visualized with accuracy/loss plots, and saved as an H5 file.
